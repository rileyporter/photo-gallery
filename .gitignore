# Dependencies — reinstalled from package.json via `npm install`, never
# committed.
node_modules

# Build output — regenerated by `npm run build`, never hand-edited.
dist
dist-ssr

# Editor/OS noise that's specific to the machine
.DS_Store
*.local

# Local environment variable files
.env
.env.local
.vscode/

# Ignore image assets in the public directory
/public/photos/
*.jpg
*.jpeg
*.png
